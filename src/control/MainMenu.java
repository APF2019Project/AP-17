package control;

import java.util.Scanner;

public class MainMenu extends Menu {
    public static MainMenu mainMenu = new MainMenu();
    public void handleCommmand(String command , Menu  Scanner scanner) {
        if (allowsCommand(command)) {
            if (command == "Profile") {
                currentMenu =
            }
        }
    }

    private MainMenu(){

    }
}
